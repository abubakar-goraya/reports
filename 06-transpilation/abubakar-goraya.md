# TRANSPILATION

## Definition
Transpilation is the type of compilation that takes high-level source code and converts it into another high-level source code by keeping the abstraction level same (Not changing the logic of code, mostly changing just the syntax). It is also called source to source compilation. 